# Environment Variables Template
# Copy this file to .env.local and fill in your actual values

# ============================================
# APPLICATION
# ============================================
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# MONGODB ATLAS
# ============================================
# Get your connection string from MongoDB Atlas: https://cloud.mongodb.com
# 1. Create a free M0 cluster
# 2. Create database user with username and password
# 3. Whitelist IP: 0.0.0.0/0 (for deployment) or your IP (for development)
# 4. Get connection string from "Connect" > "Connect your application"
# 5. Replace <username>, <password>, and add database name
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/qr-code-app?retryWrites=true&w=majority&appName=Cluster0
# After setup, run: node scripts/setup-database.js
MONGODB_URI=mongodb+srv://your-username:your-password@your-cluster.mongodb.net/qr-code-app?retryWrites=true&w=majority&appName=Cluster0

# ============================================
# AUTHENTICATION (Stage 2+)
# ============================================
# Generate a secure random string for JWT secret
# You can use: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-here
JWT_EXPIRES_IN=7d

# ============================================
# GOOGLE OAUTH (Future)
# ============================================
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ============================================
# PAYMENT (Stage 8+)
# ============================================
STRIPE_PUBLIC_KEY=your-stripe-public-key
STRIPE_SECRET_KEY=your-stripe-secret-key
STRIPE_WEBHOOK_SECRET=your-stripe-webhook-secret

# ============================================
# GAME API (Stage 9+)
# ============================================
GAME_API_KEY=your-game-api-key
GAME_API_URL=https://your-game-api.com

# ============================================
# FIGMA MCP INTEGRATION (Design to Component)
# ============================================
# Get your Figma personal access token from:
# https://www.figma.com/developers/api#access-tokens
# 1. Go to Figma Settings > Personal Access Tokens
# 2. Generate new token with read access
# 3. Copy the token here
FIGMA_ACCESS_TOKEN=your-figma-personal-access-token

# Optional: Default Figma file/project for portfolio
# FIGMA_PROJECT_ID=your-project-id
# FIGMA_FILE_ID=your-portfolio-file-id
